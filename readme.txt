1. compile and run customer.c
	-> gcc customer.c
	-> ./a.out
___ it will make a customer database with a predefined user with name "admin"
							and password "admin12345"

2. compile and run account.c
	-> gcc account.c
	-> ./a.out

___ it will make database which will keep track of normal user account

3. compile and run acc_no.c	
	-> gcc acc_no.c
	-> ./a.out

___ it will make db that'll keep record of account numbers assigned(to keep them unique)

4 compile server.c
	-> gcc server.c -o server
	->./server

5 compile server.c
	-> gcc client.c -o client
	->./client

